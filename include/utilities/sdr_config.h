#ifndef SDR_CONFIG_H_
#define SDR_CONFIG_H_

#include <stddef.h>

/* On the groundstation SDR is currently built as part of CSP using WAF, which
 * puts all the config options in the include below. When we release SDR as a
 * stand-alone entity we can put the config options here directly.
 */
//#include <csp/csp_autoconfig.h>

#endif // SDR_CONFIG_H_
